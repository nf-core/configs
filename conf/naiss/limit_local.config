
executor {
    // Cap local jobs to limit burden if not run in a job
    // larger jobs than this with `executor = local` will not run!

    $local {
        cpus   = 8
        memory = 128.GB
    }
}