//Nextflow config file for running with --no-sign-request
params {
    config_profile_description = 'AWS anonymous'
    config_profile_contact = 'Maxime U Garcia (@maxulysse)'
    config_profile_url = 'https://aws.amazon.com/'
}

aws {
    client {
        anonymous = true
    }
}
